package tools;

public class AdvenceCalc extends Calc {
    public int pow(int a, int b) {
        return (int)(Math.pow(a, b));
    }
    public int sqrt(int a) {
        return (int)(Math.sqrt(a));
    }
}
